# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
gophish
chronolure

# Compiled Object files
*.o
*.a
*.[568vq]
[568vq].out
*.cgo1.go
*.cgo2.c
_cgo_defun.c
_cgo_gotypes.go
_cgo_export.*

# Test binaries and folders
_obj
_test
_testmain.go

# Node modules and dependencies
node_modules/
package-lock.json
yarn.lock

# Python virtual environments
.venv/
venv/
env/
ENV/

# Databases
*.db
*.db-shm
*.db-wal
gophish.db*
chronolure.db*

# SSL Certificates and Keys
*.crt
*.key
*.pem
gophish_admin.crt
gophish_admin.key
chronolure_admin.crt
chronolure_admin.key

# Configuration files with sensitive data
config.json

# IDE and Editor files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Logs
*.log

# Build artifacts
dist/
build/

# Reporting module — corporate content (templates, assets, generated reports)
# must never be versioned. The repo ships only the generic engine; all
# corporate knowledge lives in the database / outside git.
/report_templates/
/reports/
/report_assets/
*.docx
*.xlsx
*.xls
*.csv
# Corporate logos / evidence screenshots (the repo keeps only its own static PNGs)
*.jpg
*.jpeg

# Local agent/session config (paths, command history) — not for the repo
.claude/

# Respaldos de plantillas corporativas (nunca al repo)
*.bak
